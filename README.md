# Sampling
In main.py, first learn the distribution by X_train (via Tucker SVD). Next, sampling N points based on the learned distribution. Compute the Energy Distance to show the similarities of X_train and the samples.
